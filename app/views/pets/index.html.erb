<h1>All Pets</h1>

<% @pets.each do |pet| %>
  <h2><%= pet.name %> </h2>
  <p><%= "#{pet.image}" %></p>
  <ul>
    <li><%= "Age: #{pet.approximate_age}" %></li>
    <li><%= "Sex: #{pet.sex}" %></li>
    <li><%= "Shelter: #{@shelters.find(pet.shelter_id).name }" %></li>
  </ul>

<% end %>
