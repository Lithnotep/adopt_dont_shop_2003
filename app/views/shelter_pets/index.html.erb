<<h1><%= @shelter.name %></h1>

<% @shelter.pets.each do |pet| %>
  <h2><%= pet.name %> </h2>
  <p><%= "#{pet.image}" %></p>
  <ul>
    <li><%= "Age: #{pet.approximate_age}" %></li>
    <li><%= "Sex: #{pet.sex}" %></li>
  </ul>
  <% end %>
